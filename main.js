function preload() {}

function setup() {
    canvas = createCanvas(600, 450);
    video = createCapture(VIDEO);
    canvas.center();
    video.size(600, 450);
    video.hide();
}

function draw() {

}